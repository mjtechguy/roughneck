[project]
name = "roughneck"
version = "0.1.0"
description = "Deploy cloud nodes for Gas Town"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.28.0",
]

[project.scripts]
roughneck = "roughneck:main"
