[project]
name = "roughneck"
version = "0.1.0"
description = "AI-assisted cloud development environment provisioner"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.28.0",
]

[project.scripts]
roughneck = "roughneck:main"

[dependency-groups]
dev = [
    "ruff>=0.14.11",
]
